# Face Reference Vectors

This directory contains vector references and SVG guidelines for the 9 Screen Weasel identities.
SVGs in this folder serve as authoring and design prototypes. At runtime, the geometric parameters are mapped into `FaceIdentity` structs for on-device procedural rasterization.
